#The /etc/rc.local file runs at boot, add these lines to the bottom of the file to fix a few things

#Bluetooth was on at boot, this turns it off. Use sudo rfkill unblock bluetooth to turn it on
rfkill block bluetooth

#Brightness skipped steps so there were only a few brightness levels
echo N | sudo tee /sys/module/video/parameters/brightness_switch_enabled

#Disable tap to click while typing, this never worked great for me so I just disabled tap to click completely in the "Mouse & Touchpad" settings. The Apple track pad is the only thing I really miss from my Mac days
syndaemon -i 4 -d -t -K